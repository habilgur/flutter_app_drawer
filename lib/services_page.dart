import 'package:flutter/material.dart';

class Services extends StatelessWidget {
  static final String pageId = "services";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("This is services page"),
      ),
    );
  }
}
